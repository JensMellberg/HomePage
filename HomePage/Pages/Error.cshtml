@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Något gick oväntat fel :(.</h2>

@foreach (var error in Model.Errors) {
    <p>@error</p>
}